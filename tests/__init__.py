"""Test package for vietnam_legal_rag.

This package is intentionally empty of business logic tests at the
skeleton stage — its purpose is to verify the project is wired up
correctly (package import, settings load, domain registry, CLI help).
Subsequent phases will add real tests against each module.
"""
